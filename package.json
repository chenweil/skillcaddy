{
  "name": "skillcaddy",
  "version": "0.4.1",
  "private": true,
  "description": "Local skill source library and per-project symlink manager.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/*.test.js",
    "pull:github": "bash scripts/pull-github.sh"
  },
  "engines": {
    "node": ">=20"
  }
}
